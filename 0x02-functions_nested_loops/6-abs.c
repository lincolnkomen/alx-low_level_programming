#include "main.h"

/**
 * _abs - gives absolute value of an integer.
 * @i: Input number
 *
 * Return: absolute value.
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
